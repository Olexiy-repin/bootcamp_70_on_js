/*
 * Властивості елемента (hero)
 * - Зображення
 * - Текст та textContent
 * https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480
 */

// const imageEl = document.querySelector('.js-hero-image');

// imageEl.alt = 'Hello';
// imageEl.src = 'https://picsum.photos/id/237/200/300';

// console.dir(imageEl);

// const heroTitleEl = document.querySelector('.js-hero-title');

// heroTitleEl.textContent = 'Welcome';

// console.dir(heroTitleEl);

/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута, значення-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

// const imageEl = document.querySelector('.js-hero-image');

// console.dir(imageEl);

// imageEl.setAttribute('height', 213);

// imageEl.removeAttribute('src');

// console.log(imageEl.hasAttribute('alt'));

/*
 * Data-атрибути
 */
// const actionBtnsEl = document.querySelectorAll('.js-action-btn');

// console.log(actionBtnsEl);

// actionBtnsEl.forEach(actionBtn => {
//   console.dir(actionBtn.dataset.action);
// });
